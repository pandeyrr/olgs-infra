apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: olgs-web-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/pandeyrr/olgs-infra.git'
    targetRevision: main
    path: webapp/overlays/dev
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: olgs-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
